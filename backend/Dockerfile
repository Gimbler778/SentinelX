FROM node:20-alpine

WORKDIR /app

# Install dependencies first (better layer caching)
COPY package*.json ./
RUN npm ci --only=production

COPY . .

EXPOSE 3000

# Run migrations then start
CMD ["sh", "-c", "node migrations/run.js && node src/index.js"]
